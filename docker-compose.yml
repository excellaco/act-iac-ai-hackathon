services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: parcela
    ports:
      - "5433:5432"
    volumes:
      - parcela_db:/var/lib/postgresql/data

volumes:
  parcela_db:
